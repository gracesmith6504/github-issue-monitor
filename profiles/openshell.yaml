repos:
  - "NVIDIA/OpenShell"

repo_language: Rust
repo_display_name: "NVIDIA/OpenShell"

calibration: |
  CRITICAL context for this repository:
  - Issues are filed by AI agents that ALWAYS include file paths, line numbers,
    and root cause analysis. File paths alone do NOT mean the fix is clear —
    score Starting Point based on whether the exact CODE CHANGE is described
    (SP=5) or just the location (SP=3).
  - This is a 21-crate Rust workspace (~8.8M lines). The number of CRATES
    touched matters more than the number of files. A change in one isolated
    crate may be trivial; a change spanning crates requires understanding
    dependency relationships that a newcomer cannot learn quickly.
  - The project has a vouch gate — first-time external contributors must get
    a maintainer to /vouch them before any PR is accepted.
  - Before scoring, search the calibration examples for a match: same crate,
    same subsystem, or same concept (e.g. "symlink" matches "symlink"). If
    you find a match, START from that example's scores and only deviate if the
    new issue is clearly easier or harder. A prior community attempt that
    failed or was rejected is the strongest signal that a task is harder than
    the issue text suggests.

architecture: |
  Subsystem isolation (least to most interconnected):
  - Docs/examples: No Rust needed. Editing EXISTING docs is always safe (Fam=4-5).
    However, NEW docs that define project standards, processes, or conformance
    criteria require Fam=2-3 (project-specific knowledge to write accurately).
  - openshell-tui (18 files): Terminal UI dashboard, isolated, few dependencies. Self-contained changes.
  - openshell-cli (17 files): CLI frontend, mostly isolated. Safe for newcomers who know Rust basics.
  - openshell-policy (3 files): Policy parsing, small and focused. Depends only on core.
  - openshell-router (5 files): Inference routing, small and focused. Depends only on core.
  - openshell-driver-docker (2 files): Simplest compute driver. Depends only on core.
  - openshell-bootstrap (9 files): Gateway registration, auth tokens. Moderate complexity.
  - openshell-supervisor-process (19 files): SSH server, process spawning, privilege drop. NOT newcomer-friendly.
  - openshell-supervisor-network (26 files): L7 proxy, OPA policy, inference routing. Hard.
  - openshell-server (59 files, depends on 12 other crates): The gateway hub. ANY issue touching this should have familiarity no higher than 2.
  - openshell-core (30 files): Foundation crate — every other crate depends on it. Changes here cascade everywhere. Familiarity should be 1.

domains: |
  Hard domains (push verdict DOWN even when file paths are given):
  - SSH/PTY handling, session lifecycle, channel management
  - Landlock/seccomp sandboxing, security policy enforcement
  - gRPC API changes touching proto definitions in proto/
  - Cross-crate refactoring or new abstractions
  - Network proxy internals, L7 policy evaluation, TLS termination
  - Concurrency, async lifecycles, race conditions
  - Multi-platform issues (macOS vs Linux driver differences)

  Easier domains (don't push down):
  - Documentation and examples (no Rust needed)
  - Build system fixes (build.rs, Cargo.toml, mise tasks)
  - CLI flag additions or changes in openshell-cli
  - TUI layout, styling, or navigation in openshell-tui
  - Single-crate bug fixes with clear error messages and isolated scope
  - Test additions following existing patterns

examples:
  - number: 832
    scores: "SP=5 Scope=5 Fam=5"
    reason: "The exact code change is described: change glob from 'v*' to 'v[0-9]*' in build.rs. You know what to type before opening the file. 1 line, no Rust knowledge."
  - number: 1339
    scores: "SP=5 Scope=5 Fam=5"
    reason: "The exact code change is described: replace '--sync' with '--upload .' in one doc file. Mechanical substitution, no codebase knowledge."
  - number: 2173
    scores: "SP=5 Scope=4 Fam=3"
    reason: "The fix approach is explicitly described: replace std::fs::rename with copy+delete at certgen.rs L680. You know the approach before opening the file (SP=5). Single crate, but implementing the fix correctly requires reading the surrounding Rust code (Fam=3)."
  - number: 2095
    scores: "SP=4 Scope=4 Fam=5"
    reason: "File and approach are both clear: add an if-check around client_ca_path in a Helm template (SP=4). No Rust, no codebase knowledge needed — it's a config template fix (Fam=5)."
  - number: 1347
    scores: "SP=3 Scope=3 Fam=3"
    reason: "Files are identified but the fix requires investigation: you need to understand gRPC validation patterns to implement the split correctly (SP=3). 2 files, 67 lines, pattern-matching required."
  - number: 1425
    scores: "SP=3 Scope=2 Fam=2"
    reason: "Root cause is diagnosed but the fix is not obvious: looked like a 2-file fix but actually needed 296 lines across 4 files in CLI internals (SP=3). Community attempt was rejected — a strong signal the task is harder than the issue suggests."
  - number: 2309
    scores: "SP=3 Scope=2 Fam=2"
    reason: "Same subsystem as #1425 (symlinks). Root cause identified but fix requires understanding upload planner internals (SP=3). Prior community rejection on #1425 anchors this as harder than the description suggests."
  - number: 2304
    scores: "SP=2 Scope=2 Fam=1"
    reason: "Problem is clear but no direction on how to decompose: splitting a 10,103-line file requires understanding every function to know where it belongs (SP=2). Scope is huge despite being 'one crate' (Scope=2). Deep architectural knowledge needed (Fam=1)."
  - number: 1358
    scores: "SP=2 Scope=1 Fam=1"
    reason: "Only symptoms described, cross-platform networking issue: 15 files changed by a core maintainer. Requires deep architectural knowledge of gateway internals (Fam=1)."
  - number: 2267
    scores: "SP=2 Scope=3 Fam=3"
    reason: "This asks someone to WRITE a new standards document from scratch — not edit an existing doc. The issue describes what the doc should cover but not the actual content. You'd need to research the project's current SDK ecosystem and make judgment calls about standards (SP=2). Moderate scope — one document, but substantial content (Scope=3). Requires understanding the project's SDK landscape well enough to define standards (Fam=3)."
  - number: 2292
    scores: "SP=3 Scope=1 Fam=2"
    reason: "This is an RFC/design proposal to create an SDK conformance framework with tiers, scoring, and automation. Despite mentioning specific files, the real work is designing the framework itself — open questions about approach make this Scope=1. You need project-specific knowledge of the SDK ecosystem and how teams use it (Fam=2)."
  - number: 2275
    scores: "SP=5 Scope=5 Fam=5"
    reason: "The exact change is described: remove a stray bracket in one doc file. Mechanical fix, no codebase knowledge (Fam=5)."
  - number: 2229
    scores: "SP=3 Scope=3 Fam=2"
    reason: "The issue describes a symptom (black screen after shell exit) but not the cause. The fix required understanding TUI redraw lifecycle and terminal state restoration — you'd need to investigate how ratatui interacts with shell subprocesses (SP=3). Done by a core maintainer in 59 changed lines (Scope=3, Fam=2)."
  - number: 1998
    scores: "SP=3 Scope=2 Fam=1"
    reason: "VM driver + init process + seccomp policy interaction. Root cause is described but fixing it required understanding how the sandbox supervisor initializes inside a VM, how kernel security policies are applied, and how egress rules propagate — deep VM driver internals (Fam=1). 6 files, 93 lines changed by a core maintainer (Scope=2)."
  - number: 2083
    scores: "SP=3 Scope=2 Fam=1"
    reason: "Rejecting JSON-RPC batches sounds contained but required a typed inspection-error refactor across relay, OPA, and proxy layers — 243 lines changed across 3 files. You need to understand how JSON-RPC messages flow through the proxy pipeline (Fam=1). The PR was much larger than the issue implies (Scope=2)."
  - number: 1696
    scores: "SP=2 Scope=1 Fam=1"
    reason: "GPU sandboxes fail because seccomp blocks memfd_secret syscall. Fixing this requires understanding the intersection of GPU initialization, kernel seccomp policies, and the sandbox security model — three hard domains at once. No clear fix path described, multiple possible approaches (SP=2, Scope=1, Fam=1)."
  - number: 2320
    scores: "SP=3 Scope=3 Fam=3"
    reason: "TUI resize bug. Location identified (openshell-tui) but fix requires investigating ratatui event handling and layout reflow (SP=3). 2-5 files in an isolated crate (Scope=3). Claude Code can learn the TUI patterns by reading the crate (Fam=3)."
  - number: 2332
    scores: "SP=4 Scope=4 Fam=3"
    reason: "Documentation task but NOT Fam=5 — this requires understanding MicroVM driver use cases and limitations to write accurately (Fam=3). Clear what to document and where (SP=4). Contained to docs (Scope=4)."
  - number: 2252
    scores: "SP=2 Scope=3 Fam=2"
    reason: "New Jupyter example requires design decisions about sandbox lifecycle and SDK integration (SP=2). Moderate scope — one example but substantial implementation (Scope=3). Needs understanding of sandbox lifecycle beyond what code reading reveals (Fam=2)."
  - number: 2290
    scores: "SP=3 Scope=3 Fam=2"
    reason: "SSH exec hardcodes /bin/bash. Bug is clear and location identifiable (SP=3), but fix touches supervisor internals — runtime behavior of SSH sessions isn't obvious from reading code (Fam=2)."
  - number: 2339
    scores: "SP=2 Scope=2 Fam=2"
    reason: "Config merged but missing from release. Debugging CI/release pipeline requires understanding build and packaging processes that aren't visible in the source code (Fam=2). No clear fix path (SP=2)."
  - number: 2343
    scores: "SP=2 Scope=2 Fam=1"
    reason: "GPU segfault in sandbox. Three hard domains at once: GPU initialization, sandbox security model, kernel/driver compatibility (Fam=1). No clear direction on the fix (SP=2)."
  - number: 2342
    scores: "SP=5 Scope=4 Fam=5"
    reason: "Helm template passthroughs with exact syntax provided — the issue spells out the {{- with }} blocks to add and the values.yaml shape. Same pattern as existing nodeSelector/affinity/tolerations already in the template (SP=5). 1-2 files, mechanical additions (Scope=4). No Rust, no codebase knowledge, standard Helm patterns (Fam=5)."
  - number: 2123
    scores: "SP=4 Scope=4 Fam=4"
    reason: "Arch Linux install path. Detailed design with exact function names and flow mirroring install_linux_rpm(), but the actual code isn't spelled out (SP=4, not 5). Single file change (Scope=4). Shell scripting, no Rust — Claude Code can read existing install functions and replicate the pattern, but you need to understand the install flow (Fam=4, not 5)."

auto_label: false

label_map:
  "JUMP ON IT": "good first issue"
  "GO FOR IT": "good first issue"

verdict_thresholds:
  "JUMP ON IT": 13
  "GO FOR IT": 10
  "STRETCH": 7
  "LONG SHOT": 5
