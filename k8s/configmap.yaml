apiVersion: v1
kind: ConfigMap
metadata:
  name: issue-monitor-config
  namespace: issue-monitor
data:
  WATCH_REPOS: "org/repo"
  NOTIFY_REPO: "your-username/github-issue-monitor"
  POLL_INTERVAL: "30"
  LLM_PROVIDER: "github"
  LLM_MODEL: "gpt-4o"
